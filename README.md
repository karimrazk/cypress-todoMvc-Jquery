## cypress-todoMvc-Jquery

This repo contains an example of todomvc in Jquery, with the tests written in Cypress.

 http://todomvc.com/examples/jquery/
 
 To run this app :
 1. `npm install`
 2. `npm run cypress`
